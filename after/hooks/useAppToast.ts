import React from 'react'

const useAppToast = () => {

  const toastMsg = null ,toastSev = null;

  const showToast = () => {
    //logic to when to show toast
  }

  //additional logic

  return { toastMsg, toastSev, showToast }
}

export default useAppToast;